I am the edit page

<%= form_tag "/client/buttons/#{@button['id']}", method: :patch do %>
  <div>
    Shape:<input type="text" name="input_shape" value="<%= @button['shape'] %>">
  </div>
  <div>
    Color: <input type="text" name="input_color" value="<%= @button['color'] %>">
  </div>
  <div>
    Description: <input type="text" name="input_description" value="<%= @button['description'] %>" />
  </div>
  <div>
    Image URL: <input type="text" name="input_image_url" value="<%= @button['image_url'] %>" />
  </div>
  <input type="submit" value="Edit a currently existing button">
<% end %>



<p>If you click the button below, you will delete this item</p>

<!-- somehow make a delete request -->
<a href="/client/buttons/<%= @button['id'] %>" data-method="delete">Delete</a>
